
# Skip commitlint for automated commits (e.g. changesets "Version Packages")
if grep -q "\[skip ci\]" "$1"; then
  echo "⏩ Skipping commitlint for [skip ci] commit"
  exit 0
fi

npx --no-install commitlint --edit "$1"