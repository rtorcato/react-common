---
title: react-common
description: The @rtorcato React monorepo — home of @rtorcato/common-react and @rtorcato/react-hooks.
sidebar_position: 0
slug: /
---

# react-common

**react-common** is the `@rtorcato` React monorepo. It publishes two packages to
the public npm registry:

- [`@rtorcato/common-react`](https://www.npmjs.com/package/@rtorcato/common-react) —
  a React 19 component library built on shadcn/ui, Radix UI, TypeScript, and
  Tailwind CSS v4.
- [`@rtorcato/react-hooks`](https://www.npmjs.com/package/@rtorcato/react-hooks) —
  headless React hooks with zero UI dependencies.

:::info Docs are under construction
This site is being built out — track progress in the
[react-common issues](https://github.com/rtorcato/react-common/issues).
:::

## Install

```bash
pnpm add @rtorcato/common-react
```
