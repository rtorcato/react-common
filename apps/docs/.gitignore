# build output
.docusaurus/
build/

# Storybook colocated for local StorybookEmbed previews (pnpm storybook:local).
# In prod it's copied into build/storybook by docs.yml instead.
static/storybook/

# typedoc-generated API reference (regenerated on build) — see #9
docs/api/

# synced from root CHANGELOG.md on build — see #10
docs/changelog.md

# dependencies
node_modules/

# logs
npm-debug.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# Playwright outputs — see #11
test-results/
playwright-report/
playwright/.cache/

# macOS-specific files
.DS_Store
