# Registry routing only — NO auth tokens here.
# pnpm refuses to expand ${ENV} secrets from a committed project .npmrc, so
# auth lives in a user-level ~/.npmrc (CI writes it; locally it's your own).
@rtorcato/js-tooling:registry=https://gitlab.com/api/v4/projects/60855075/packages/npm/
@rtorcato/js-common:registry=https://gitlab.com/api/v4/projects/69645966/packages/npm/
