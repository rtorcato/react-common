---
"@rtorcato/shadcn-ui": minor
---

Add `ThemeToggle` (ui-extended): a light/dark toggle built on `next-themes` and the `Button` primitive. Pairs with `ThemeProvider`; token-based styling, SSR-safe (renders a stable icon until mounted), accessible label. Importable from `@rtorcato/shadcn-ui/components/ui-extended/theme-toggle`.
