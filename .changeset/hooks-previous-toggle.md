---
"@rtorcato/react-common": minor
---

Add two headless React hooks: `usePrevious` (the value from the previous render) and `useToggle` (boolean state with stable `toggle`/`setOn`/`setOff` helpers).
